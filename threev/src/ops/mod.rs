/* use super::types::{Key, FactValued, DoubleValued};
use std::{io, fs};

pub trait Executable {

}
#[derive(Debug)]
pub enum Ops {
    Get(GetOp),
    Delete(DeleteOp),
    Set(SetOp),
    CreateAttr(CreateAttr),
}
 */
